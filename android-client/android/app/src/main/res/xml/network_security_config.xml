<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 企业内网为明文 HTTP，允许明文流量（仅限内网使用） -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
